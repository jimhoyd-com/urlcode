**Tried:** test a create -> read -> update -> restart lifecycle with `urlcode test`.
**Expected:** multi-step fixtures.
**Got:** fixtures are single requests with no captured values, so both runs added a separate `node:test` harness that spawns `npx urlcode serve` (slow; killing `npx` can orphan the server; run 2 copied the project to a temp dir to isolate data). Run 1 also found `urlcode test` prints noisy JSON request logs by default.
**Suggestion:** fixture step chaining with captured variables and a restart step; an in-process test helper (start server on port 0 with data isolation); quiet output by default with `--verbose`.
**Evidence:** an A/B benchmark on URLCode 0.4.1 with Sonnet 5 agents building the same Todo app from the repo docs. Run 1 (benchmark Agent B, no authoring skill, no browser check) and run 2 (session "Todo application with URLCode", authoring skill loaded, tightened prompt) hit this independently. n=1 each; single small app, so treat frequency as anecdotal. The Agent A control (no URLCode) needed 2 shell calls and 0 dependencies for the same app.
