**Tried:** start from `urlcode recipes add static-plus-api` and add a function that writes a file.
**Expected:** a recipe/audit path that leads straight to a passing audit.
**Got:** `audit` advised that routes with a body policy declare neither `sandbox: true` nor `sandboxReason`. `recipes/static-plus-api/urlcode.yaml` contains neither (only `recipes/webhook-receiver` shows `sandboxReason`), so the agent found the field by grepping docs and `urlcode schema route`. A persistent app cannot be sandboxed (no filesystem), so it is always the trusted case.
**Suggestion:** show `sandboxReason` in `static-plus-api`; make the advisory print the exact YAML line to add; document that anything touching the filesystem must be trusted.
**Evidence:** an A/B benchmark on URLCode 0.4.1 with Sonnet 5 agents building the same Todo app from the repo docs. Run 1 (benchmark Agent B, no authoring skill, no browser check) and run 2 (session "Todo application with URLCode", authoring skill loaded, tightened prompt) hit this independently. n=1 each; single small app, so treat frequency as anecdotal. The Agent A control (no URLCode) needed 2 shell calls and 0 dependencies for the same app.
