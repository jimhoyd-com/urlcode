**Tried:** confirm which version the docs describe.
**Got:** on origin/main (9092b4b): `llms.txt` says this revision is `0.4.0-alpha.2` (newest alpha on npm `0.4.0-alpha.1`), `docs/YAML-GUIDE.md` says it targets `0.3.0`, and the package and npm latest are `0.4.1`. An agent cannot tell which statements apply to the installed runtime; the authoring skill warns that docs and runtime must match.
**Suggestion:** generate version strings in `llms.txt`/guides from package.json and add a CI check.
**Evidence:** an A/B benchmark on URLCode 0.4.1 with Sonnet 5 agents building the same Todo app from the repo docs. Run 1 (benchmark Agent B, no authoring skill, no browser check) and run 2 (session "Todo application with URLCode", authoring skill loaded, tightened prompt) hit this independently. n=1 each; single small app, so treat frequency as anecdotal. The Agent A control (no URLCode) needed 2 shell calls and 0 dependencies for the same app.
**Not verified:** whether other docs carry stale versions.
