**Tried:** validate the JSON body (title 1-200 chars string, optional description, boolean completed) and the path `id`.
**Expected:** declare it in YAML.
**Got:** only JSON syntax, media type and size are declarative. Body rules (~25 lines) and the id format check live in code; path parameter schemas accept `minLength/maxLength` but no `pattern`/`format`, so a UUID check is hand-written.
**Suggestion:** a JSON Schema subset for `request.body` returning a structured 400/422, and `pattern`/`format` on parameter schemas.
**Evidence:** an A/B benchmark on URLCode 0.4.1 with Sonnet 5 agents building the same Todo app from the repo docs. Run 1 (benchmark Agent B, no authoring skill, no browser check) and run 2 (session "Todo application with URLCode", authoring skill loaded, tightened prompt) hit this independently. n=1 each; single small app, so treat frequency as anecdotal. The Agent A control (no URLCode) needed 2 shell calls and 0 dependencies for the same app.
**Not verified:** interaction with sandboxed routes' snapshot/limits.
