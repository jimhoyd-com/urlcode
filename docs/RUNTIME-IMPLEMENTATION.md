# Implementing the URLCode contract

This is a contributor directive and implementation map for people and agents
building or changing a URLCode runtime. It turns the project-format contract
into small, language-neutral units of work. It is not a second source format,
an SDK specification, or a claim that another runtime already exists.

The normative behavior remains the [implemented specification](SPECIFICATION.md),
the [JSON Schema](../schemas/urlcode.schema.json), and the focused topic
contracts they link to. If this guide conflicts with one of those sources, fix
this guide; do not make a runtime choose between them.

This file is contributor documentation. It is deliberately not included in
`llms-full.txt`, the npm tarball, or the production container image. The guide
must not be copied into runtime source comments merely to make it visible to an
agent: this repository's type-stripping build preserves source comments in
`dist/`.

## Directive

> Implement URLCode as a checked behavioral contract, not as a translation of
> TypeScript. A runtime must either reproduce a declared capability and pass its
> fixtures, or refuse that capability before serving.

When changing a core semantic boundary, maintainers must:

1. identify the affected contract ID below;
2. update the implementation card and the authoritative semantic document;
3. add or update a behavior fixture at the narrowest relevant layer; and
4. preserve explicit refusal where a target or runtime cannot provide the
   behavior.

Do not add a prose prompt beside every implementation function. Such prompts
drift, make deployed artifacts larger, and cannot prove equivalence. Keep the
portable instruction here, link it to the source symbol, and use executable
fixtures as the proof.

## What is portable

The YAML-defined behavior is the portability target: validation, normalization,
route selection, input parsing, declared responses, redirects, assets,
conditions, policies, and refusal behavior where a runtime implements them.

Project `function` and `middleware` modules are different. The trusted default
means JavaScript executed in-process with ordinary Node access. Another runtime
does **not** implement that by translating the module or pretending its host
access is equivalent. It must either provide an explicitly documented compatible
guest mode or refuse that route before activation. The existing `sandbox: true`
QuickJS/WASM behavior is also a separate, bounded execution contract, not a
general promise that arbitrary code is portable. See [function security](FUNCTION-SECURITY.md).

Operator bindings, host plugins, extension registration, deployment transport,
filesystem layout and resource limits are host-owned. An alternate runtime must
state its own operational limits and must never let project YAML grant itself
authority.

## Contract cards

Each card is a useful unit for an implementation agent. The card says what to
build; the linked specification says every accepted value and edge case.

<!-- runtime-contract-map:start -->
| ID | Source seam in this runtime | Implementer task | Primary evidence |
|---|---|---|---|
| `RIM-CFG-001` | `packages/core/src/config.ts`: `parseYaml` | Parse the restricted YAML 1.2 JSON-value profile; reject ambiguity and unsafe YAML before schema validation. | [Files and validation](SPECIFICATION.md#files-and-validation), `test/config.test.ts` |
| `RIM-CFG-002` | `packages/core/src/config.ts`: `validateDocument`, `loadDocumentInWorker`, `normalizeRouteAuth`, `extensionPolicyError`; `packages/core/src/extensions.ts`: `checkExtensionPolicies` | Validate the schema, resolve includes and shared blocks, and normalize short forms before any consumer sees a route. The route `auth:` short form is only a mapping: `true` is `{}`, an object becomes `policies.extensions.auth` minus `required`, and `required: false` emits nothing; core must not validate the object's other keys, which the auth extension's `policySchema` owns. Record which routes used the short form so a policy-schema failure (at validate time and at startup) is located at the route's `auth` key rather than the expansion, and check the keys written beside `required: false` too. On request, record the file (`urlcode.yaml` or the include path as written) each route and extension declaration came from, so authoring tools that follow includes reuse this resolution and root containment instead of their own. | [Files and validation](SPECIFICATION.md#files-and-validation), [fixture suggestions](TOOLING.md#fixture-suggestions), [shared blocks](SPECIFICATION.md#shared-blocks), [auth short form](EXTENSIONS.md#protecting-a-route-the-auth-short-form), `test/config.test.ts`, `test/extensions.test.ts` |
| `RIM-CAP-001` | `packages/core/src/capabilities.ts`: `analyzeProjectCapabilities`, `assertTargetCompatibility` | Inspect the fully normalized project and refuse each unsupported capability with its route named before activation. A capability whose configuration a target cannot honor (non-default `policies.compression` on Vercel/AWS/Cloudflare) is refused the same way, not silently accepted and dropped; a capability that is genuinely enforced but only with a caveat the analysis cannot verify (`policies.throttle`'s `partition: route` on Vercel/AWS, which is real per-instance in-process enforcement, not `native`, because its effective quota is quota × instance count) is reported `delegated` with that caveat named, never `native`. | [Capabilities](CAPABILITIES.md), [policies](POLICIES.md#portability-and-the-per-target-table), `test/capabilities.test.ts`, `test/policy-compression.test.ts` |
| `RIM-COMPILE-001` | `packages/core/src/router.ts`: `compileRoutes` | Compile validated declarations into a deterministic route table; reject ambiguous routes, invalid bindings, and invalid handler combinations before serving. | [Routes](SPECIFICATION.md#routes), `test/config.test.ts`, `test/http.test.ts` |
| `RIM-MATCH-001` | `packages/core/src/match.ts`: `parseTarget`, `matchRoute` | Decode a request target once and select exactly one route by the documented precedence; match before method admission. | [Routes](SPECIFICATION.md#routes), [routing](ROUTING.md), `test/match.test.ts` |
| `RIM-INPUT-001` | `packages/core/src/match.ts`: `contextFor`, `redirectLocation` | Parse and validate declared path/query/header inputs, apply defaults only when absent, and assemble redirects from explicit values only. | [Inputs](SPECIFICATION.md#inputs), [redirects](SPECIFICATION.md#redirects), `test/match.test.ts` |
| `RIM-PATTERN-001` | `packages/core/src/pattern-guard.ts`: `assertSafePattern`, `backtrackingPaths` (re-exported from `@jimhoyd/urlcode/extensions` for workspace packages); `packages/core/src/policies/agents.ts`: `validatePattern`, `onRequest`; `packages/forms/src/forms.ts`: `validateFlow` | Admit an author regex (parameter and body `pattern`; agents `denyPatterns`, `allowPatterns` and list entries; forms field `pattern`) only when its worst-case matching cost is bounded: refuse repeated quantified groups, lookaround and backreferences, cap unbounded quantifiers, charge every variable-width quantifier (`*`, `+`, `?`, `{n,}`, `{n,m}` with `m > n`) and alternation against one backtracking-path budget, and bound the input (128 characters for parameters, bodies and forms fields; only the first 512 bytes of `User-Agent` are matched). Refuse the same patterns on every target, even one with a linear-time engine, so a project validates identically everywhere. A workspace package that accepts an author-supplied pattern must call this shared guard rather than keep its own copy, so admission cannot drift. | [HTTP](HTTP.md), [agents policy](policies/agents.md#the-pattern-subset), `test/body-validation-limits.test.ts`, `test/policy-agents.test.ts`, `packages/forms/test/forms.test.ts` |
| `RIM-CONDITION-001` | `packages/core/src/conditions.ts`: `normalizeMatch`, `assertDisjointMatches`, `matchesRoute` | Normalize exact conditions, reject overlapping conditional branches, and use the configured origin rather than client host text. | [Exact conditions](SPECIFICATION.md#exact-conditions-and-duplicate-path-alternatives), `test/conditions.test.ts` |
| `RIM-HTTP-001` | `packages/core/src/http-policy.ts`: `compileHttp`, `checkRequest`, `decorateResponse` | Enforce body admission and response-header ownership before and after a handler without silently relaxing limits. A route with a JSON `request.body.schema` answers a failing body with a JSON 422 listing every issue, naming only schema-declared paths and identifier-shaped undeclared properties, never a value. Body-schema string lengths go up to the 1 MiB request body limit; a `pattern` node keeps the 128-character regex-input cap. | [HTTP configuration](SPECIFICATION.md#http-requestresponse-configuration), [HTTP](HTTP.md), `test/http-policy.test.ts`, `test/body-validation-json.test.ts`, `test/body-validation-limits.test.ts` |
| `RIM-POLICY-001` | `packages/core/src/policies.ts`: `effectivePolicies`, `compilePolicies`, `policyRequest` | Merge profile/project/route policy layers, compile only enforceable policies, and run their request/response/error hooks in the declared order. | [Policies](SPECIFICATION.md#policies), [policy pipeline](POLICIES.md), `test/policies.test.ts` |
| `RIM-ASSET-001` | `packages/core/src/assets.ts`: `compileAssets`, `assetResponse` | Snapshot declared assets at activation and serve the selected asset with its documented HTTP semantics; a missing selected mount asset does not fall through. | [Assets](ASSETS.md), [Routes](SPECIFICATION.md#routes), `test/assets.test.ts` |
| `RIM-HOST-001` | `packages/core/src/client-address.ts`: `isLoopbackAddress`, `loopbackHostCheck`; `packages/core/src/server.ts`: `startServer` | When the self-hosted server's bound address is loopback (127.0.0.0/8, `::1`, IPv4-mapped 127.x; a `localhost` bind resolves to one of these), refuse every request, probes and metrics included, before routing unless it carries exactly one `Host` header whose value, compared case-insensitively and exactly, is `localhost`, `127.0.0.1`, `[::1]` or the bound address literal on the bound port (a bare name only when that port is 80), or the configured `--origin` authority or an operator `--alias-origin` authority (with no explicit port it is admitted bare or with the scheme's default port; with one, only with that port). An absolute-form request target's authority must pass the same test. The refusal is 421 with the fixed body `Misdirected request`, never reflects the header, and is logged as an ordinary request record. A non-loopback bind is not checked, and platform-fronted targets (AWS, Vercel, Cloudflare) do not apply it. This is a DNS-rebinding defence, not authentication. | [Operations](OPERATIONS.md#host-admission-on-a-loopback-bind), `test/host-admission.test.ts` |
| `RIM-DISPATCH-001` | `packages/core/src/runtime.ts`: `createRuntime` | Activate a complete immutable snapshot, then run request parsing, matching, admission, policies, handler dispatch, response decoration and error handling in order. | [Specification](SPECIFICATION.md), [readiness](READINESS.md), `test/http.test.ts` |
| `RIM-GUEST-001` | `packages/core/src/trusted-functions.ts`: `TrustedFunctions`; `packages/core/src/functions.ts`: `FunctionPool` | Keep trusted Node execution and `sandbox: true` execution distinct. Do not substitute one for the other, and do not claim either is portable to an unrelated host. A failed trusted call answers the generic 502/504; its source, thrown message and stack go only to the opt-in operator diagnostics channel, never the response or event log. | [Functions](SPECIFICATION.md#functions), [function security](FUNCTION-SECURITY.md), `test/trusted-functions.test.ts`, `test/dev-diagnostics.test.ts`, `test/sandbox.test.ts` |
| `RIM-ADDON-001` | `packages/core/src/addon-manifest.ts`: `parseAddonManifest`, `readAddonManifest`, `withRequirements`, `parseDescriptor`, `buildAddonCatalog`, `parseAddonCatalog`, `readAddonCatalog`; `packages/core/src/addon-install.ts`: `addAddons`, `removeAddon`, `listAddons`, `assertInertArtifact`, `assertInertArtifacts`, `artifactLockProblem`, `dependencyTree`, `rollBack`, `validateDeclaredExtensions`, `yamlInsertEntry`, `yamlAppendItem`, `yamlDelete`; `packages/core/src/upgrade.ts`: `upgradeSite`; `packages/core/src/host.ts`: `composeHost`, `orderByRequires`; `packages/core/src/operator-host.ts`: `loadOperatorHost`, `hostRevisionPin` | Core is the only install catalog: its own `addons.json` pins every add-on (kind, `requires`, release URL, sha512), so trust in core's provenance extends to the add-ons and nothing in a project selects one. Beside it, `addon-catalog.json` is release-wide agent discovery metadata built from every add-on descriptor (name, kind, package, version, description, `requires`, the descriptor's bounded `agent` block), sorted by name, every entry at core's version; reading it opens that one file and never imports, fetches, installs or activates an add-on, and it is never evidence that a project installed one. `add` installs an add-on and its transitive `requires` once each at the site's top level with `npm install --ignore-scripts`, refuses a lock entry whose integrity or URL differs from the pin and any nested copy, refuses a non-inert artifact (only the allowed data files; a `package.json` holding only `name`, `version`, `description`, `keywords`, `homepage`, `bugs`, `license`, `author`, `contributors`, `repository`, `private` and `files`, an allowlist rather than a blocklist; a lock entry declaring no dependencies, peers, binary or install script) and, when only artifacts are added to a site that had a lock, any new lock entry other than theirs, and for an extension writes only what its `scaffold` returns (the capability; with `--example`, its optional `example` deep-merged on top, a colliding route refused; config block, `app/routes/<name>.yaml`, operator files outside `app/` that never overwrite, one `host.mjs` import and entry); `add` and `remove` splice only the `extensions.<name>` entry and `includes` item into `app/urlcode.yaml` at their source ranges, leave every other byte unchanged, and refuse (never re-serialise the document) when the result would not parse to the same data as the equivalent document edit; any failure of `add` or `remove` restores every touched file and then, once npm has run, `node_modules` (`npm ci --ignore-scripts` from the restored lock, or removal of every top-level entry the command created when there was no lock), and reports a reinstall that itself fails rather than hiding it. `upgrade` refuses a site without a lock before changing anything, applies the same artifact inertness checks to every installed artifact at its new pin, and rolls back through the same path (`npm ci --ignore-scripts` from the restored lock, a failed reinstall reported with that command). `remove` refuses while a dependant is installed or the project still uses the extension outside its own routes file, and never deletes data or operator files. `list --strict` fails on pin mismatch, nested copies, drift between `package.json`, `urlcode.yaml` and `host.mjs` (declared but not imported, or imported but not declared; an installed extension that neither names is listed with `mode: "library"` and checked only against its pin and for nested copies), or a non-inert artifact. `composeHost` reads the reviewed revision pin once (the verified `--policy` revision that `loadOperatorHost` exposes only while it imports the host file when a CLI command gets both `--policy` and `--host-file`, otherwise `PROJECT_SHA256`; both present and different, or a policy for another project revision once the host registers an extension, refuses; project YAML never supplies it), orders by `requires`, activates each extension once, exposes only declared requirements through `get`, collects `contributes` (an optional edge the descriptor records as `contributes: [<target>]`; the target need not be installed), refuses a registration whose pin or schema differs from the definition, and closes in reverse order. Validation without a host checks declared configuration and route policies against installed descriptors without running extension code. Preserve the separate `sandbox: true` route contract unchanged. | [Extensions](EXTENSIONS.md#add-ons-extensions-and-artifacts), [function security](FUNCTION-SECURITY.md), `test/addons.test.ts`, `test/addon-catalog.test.ts`, `test/upgrade.test.ts`, `test/addons.integration.ts`, `test/extensions.test.ts` |
| `RIM-OUTPUT-001` | `packages/core/src/http-response.ts`: `prepareResponse`, `errorResponse` | Produce a bounded HTTP response, preserve separate cookies, remove hop-by-hop headers, and suppress a HEAD body without changing handler selection. Content-Length is always the UTF-8 byte length of the body sent; a handler-stated length is honoured only on HEAD, where it reports what GET would send. A header repeated in the assembled result is written as separate wire lines, never collapsed to only its last value; the two runtime-owned singleton headers (`X-Content-Type-Options`, `X-Request-Id`) are the one exception, always exactly one line each regardless of what a handler set. | [Functions](SPECIFICATION.md#functions), [HTTP](HTTP.md#responses), `test/http.test.ts`, `test/response-length.test.ts` |
| `RIM-EXT-CONTEXT-001` | `packages/core/src/extensions.ts`: `extensionContextHeaderPrefix`, `isReservedContextHeader`, `stripReservedContextHeaders`; `packages/core/src/runtime.ts`: `createRuntime`; `packages/core/src/proxy.ts`: `validateProxy` | Reserve the `x-urlcode-context-*` header namespace as the one channel an `ExtensionInstance.authorize()`/`middleware()` hook can use to hand data forward into a route's own trusted `function`/`middleware` context: strip that namespace from every inbound request's headers before any extension or guest code observes it (so a client can never inject or spoof a value there), build `ExtensionRequest.headers` from that stripped clone so a hook can write into it, and build the guest-facing `guestHeaders`/`FunctionContext` from `ExtensionRequest.headers` — not from the original request headers — so a written value carries forward. Declared `credentialHeaders` (`cookie`, `authorization`, any extension-declared name) are stripped from that guest-facing projection exactly as before; this namespace is not an exemption from that rule, and an extension must never write a raw session/bearer credential into it — only a derived value. The channel stops at the guest: `validateProxy` refuses any `proxy.requestHeaders`/`responseHeaders` entry naming a reserved-namespace header, the same way it already refuses a credential-shaped name, so a route can never opt this namespace into forwarding to an external upstream. | [Extensions](EXTENSIONS.md#bearerapi-key-routes), `SECURITY.md`, `test/extension-context-header.test.ts` |
| `RIM-EXT-PRINCIPAL-001` | `packages/core/src/extensions.ts`: `installPrincipalSlot`, `validatePrincipal`, `principalIdPattern`, `prepareExtensions`; `packages/core/src/runtime.ts`: `createRuntime` | Carry one opaque, authenticated principal per request, set only by an operator-installed extension whose registration declares `providesPrincipal: true`, only from its own `authorize()` on a route whose effective `policies.extensions` names it, and only while the runtime is awaiting that call (`setPrincipal({id})`). Accept exactly a plain object whose only own key is `id`, a string matching `^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$`; freeze it as `{id, provider}` with `provider` stamped by core. Commit it only when that `authorize()` allows the request; discard it on a denial or a throw. Refuse (throw, failing the request) a set from a non-providing extension, from `middleware()`/`handle()`, after `authorize()` returned, twice in one call, or once another extension's principal is committed. Expose it read-only as `ExtensionRequest.principal` (`null` when unset) to later `authorize()`, every `middleware()` and the mount's `handle()`; never derive it from a header, cookie, query, body or YAML, and never project it into guest code. At activation give each extension `principalMounts`, the subset of its mounts whose route names a principal-providing extension, and refuse activation of a principal-providing registration that guards a route without `authorize()`. A consumer that needs a principal refuses an absent `principalMounts` entry at activation and a `null` principal per request. | [Extensions](EXTENSIONS.md#request-principal), [store ownership](STORE.md#per-record-ownership), `test/extension-principal.test.ts`, `packages/auth/test/principal.test.ts`, `packages/store/test/ownership.test.ts` |
| `RIM-EXT-ORIGIN-001` | `packages/core/src/site-origins.ts`: `siteOrigins`, `isSiteOrigin`; `packages/core/src/runtime.ts`: `createRuntime`; `packages/core/src/adapters.ts`: `resolveAliasOrigins`, `resolvePasskeyRpId`; `packages/core/src/site-origins.ts`: `passkeyRpId` | Accept one operator-set, site-wide list of alias origins (CLI `--alias-origin`, `aliasOrigins` option, `URLCODE_ALIAS_ORIGINS` on hosted adapters; never project YAML) and refuse it before loading the project unless every entry is an absolute `https:` origin (loopback `http:` allowed) with no path, query, fragment, credentials or `*`, there are at most 16, and a canonical origin is set. Serialize (lower-case scheme/host, drop a default port) and deduplicate; the canonical origin is always first. Hand the frozen list to every extension activation as `origins` beside the unchanged canonical `origin`, and expose one match, `isSiteOrigin`, that admits only a bare origin whose serialized form is in the list (`null`, a missing value, another scheme/port or a sibling host never match). Absolute URLs keep using the canonical origin. Core never names an extension; each extension keeps its own rule for a missing `Origin`. Optionally accept one operator-set shared passkey relying-party ID (CLI `--passkey-rp-id`, `passkeyRpId`, `URLCODE_PASSKEY_RP_ID`; never YAML) and refuse it before loading the project unless it is a lowercase ASCII DNS name (at most 253 characters), not an IP address, not a single label (`localhost` only when the canonical host is `localhost`), not in the documented built-in public-suffix list, and equal to or a label-boundary parent domain of the host of the canonical origin and of every alias origin; hand it to activations as `passkeyRpId` only when set. An extension running WebAuthn uses it as the RP ID and may accept any entry of `origins` as the client origin; unset, it keeps the canonical host and canonical origin. | [Extensions](EXTENSIONS.md#site-origins-and-same-origin-checks), [shared passkey RP ID](EXTENSIONS.md#shared-passkey-relying-party-domain), [operations](OPERATIONS.md#process-deployment), `test/site-origins.test.ts`, `packages/mcp/test/mcp.test.ts`, `packages/forms/test/forms.test.ts`, `packages/store/test/store.test.ts`, `packages/auth/test/auth-http.test.ts`, `packages/auth/test/protocols.test.ts`, `packages/auth/test/passkey-handler.test.ts`, `packages/admin/test/admin-http.test.ts` |
| `RIM-EXT-REQUEST-001` | `packages/core/src/router.ts`: `compileRoutes`; `packages/core/src/runtime.ts`: `createRuntime`; `packages/core/src/extensions.ts`: `extensionHookContext`, `loadExtensionHooks` | Give every request one request id: the host's `X-Request-Id` value passed into `Runtime.handle` (generated when a caller passes none, refused when empty or longer than 128 characters), carried unchanged into `ExtensionRequest.requestId` and every function/middleware context as `context.requestId`, trusted and `sandbox: true` alike; only that plain string crosses into the sandbox. Compile an `extension:` mount's `env` block with exactly the function-route rules (revision-pinned `permissions.routes[pattern].env` grant, `default` fallback, `value` literals, the same `binding-denied` refusal) and hand the frozen result to the extension as `ExtensionRequest.env`, empty when none is declared; keep `secrets`, guest `middleware` and `parameters` refused on an extension route. Call every loaded extension hook as `hook(input, context)`, where `context` is a frozen copy of `{requestId, env}` (`requestId: null`, empty `env` for a hook not run on behalf of a request) that a package may extend with its own fields; refuse a call with a malformed context. The env grant is an injection convenience for trusted code, not an access boundary; a target that cannot run extensions refuses them before serving. | [Extensions](EXTENSIONS.md#request-context-route-env-and-request-id), [function security](FUNCTION-SECURITY.md#granting-selected-bindings), `test/extension-request-context.test.ts`, `packages/mcp/test/context.test.ts` |
<!-- runtime-contract-map:end -->

The `test/runtime-implementation-contract.test.ts` guard verifies that every
source seam named above still exists. It does not prove semantic equivalence;
the focused fixtures do that work.

## Prompt shape for an implementation agent

Give an agent one card at a time. A good prompt is short because the contract
and fixtures carry the detail:

> Implement `RIM-MATCH-001` in the target language. Read the card, the linked
> specification section, and its fixtures. Preserve all stated rejection
> behavior and route precedence. Do not add new YAML fields or emulate a
> capability the target cannot enforce. Return the implementation plus every
> fixture result; label unsupported behavior as refusal.

The agent should not be asked to reproduce the TypeScript module structure or
to infer semantics from a happy-path example. A maintainer reviews the contract
diff, the focused implementation, and the fixture results.

## Building another runtime incrementally

Start with the smallest useful declarative profile: `respond`, `redirect`,
path matching, declared input validation, and the request/response rules those
handlers need. Run the shared fixtures against the existing Node runtime first,
then against the new runtime.

Add one capability only when its card, fixtures, rejection paths and operational
ownership are clear. Until then, reject activation with the route and capability
named. Do not call a runtime fully compatible because it accepts the schema; it
is compatible only for the capabilities it demonstrably implements.
