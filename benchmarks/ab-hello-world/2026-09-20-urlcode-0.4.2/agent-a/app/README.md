# Hello World

Minimal Node.js web app using only the built-in `http` module (no dependencies).

## Requirements
Node.js 18+

## Install
No packages to install.

## Run
    npm start

Then open http://localhost:3000. Set `PORT` to use another port (e.g. `PORT=8080 npm start`).
