name: release admin
on:
  workflow_dispatch:
    inputs:
      version:
        description: Exact stable or alpha version
        required: true
        type: string
      consume_changesets:
        description: Consume pending admin Changesets
        required: true
        default: true
        type: boolean
jobs:
  release:
    uses: ./.github/workflows/release-dispatch.yml
    with:
      package: admin
      version: ${{ inputs.version }}
      consume_changesets: ${{ inputs.consume_changesets }}
    secrets:
      RELEASE_AUTOMATION_TOKEN: ${{ secrets.RELEASE_AUTOMATION_TOKEN }}
