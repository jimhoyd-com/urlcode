---
"@jimhoyd/urlcode": patch
---

Add `urlcode docs search TEXT`, the CLI equivalent of MCP `search_docs` (at most three bounded excerpts), and a function-handler section in `llms.txt` covering the `(request, {args, env, route})` signature and `args` binding.
