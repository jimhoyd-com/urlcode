# Coordinated 0.4.1

The maintainer explicitly selected this stable version for core and all extensions, targeting the npm `latest` channel. The local release preparation helper applied it directly, including core (which is not a Changesets workspace). Independent package versioning remains enabled. Changesets prerelease mode is exited; historical alpha tags and the alpha channel are preserved.

Consumed Changesets: admin-kit-only-console.md, auth-kit-only-render.md, auth-store-startup-diagnostics.md, pin-generated-site-dependencies.md, ui-scaffold-peer-registration.md.
