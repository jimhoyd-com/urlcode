version: "1"
routes:
  /go:
    redirect:
      url: https://example.com
  /product/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: {type: string, minLength: 1, maxLength: 80}
    redirect:
      url: https://example.com/products/{id}
